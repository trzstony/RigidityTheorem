import Approximate_Rigidity.RigidityTheorem
import Quantum.TraceDistance
import Quantum.POVM

/-
Root module for the `RigidityTheorem` package.

The project also exposes `Quantum` and `Approximate_Rigidity` as standalone
library roots via `lakefile.toml`; this file is the aggregate imported by
`Main.lean` and the default executable target.
-/

def hello := "RigidityTheorem"
